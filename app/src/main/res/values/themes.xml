<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="SpectrumTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colors-->
        <item name="colorPrimary">@color/spectrumPrimaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColor</item>
        <item name="colorAccent">@color/spectrumSecondaryColor</item>
        <item name="colorOnPrimary">@color/textPrimaryColor</item>

        <!--Drawer-->
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/SpectrumContextualAppBar</item>
        <item name="actionModeCloseDrawable">@drawable/ic_close</item>

        <!--Typography-->
        <item name="android:fontFamily">@font/poppins_semibold</item>
        <item name="fontFamily">@font/poppins_semibold</item>
    </style>
</resources>
